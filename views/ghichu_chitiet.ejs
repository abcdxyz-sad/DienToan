<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
        <%= title %> - ThinkBoard
    </title>

    <!-- Bootstrap + Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet" />

    <style>
        body {
            background-color: #121212;
            color: #f1f1f1;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .card {
            background-color: #1e1e1e;
            border-radius: 10px;
            border: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.5);
        }

        .card-header {
			border-bottom: 1px solid rgb(0, 255, 255);
			background: #12121293;
			color: white;
		}

        .form-label {
            color: #ddd;
        }

        .form-control,
        .form-control[readonly] {
            background-color: #2c2c2c;
            border: 1px solid #444;
            color: #fff;
        }

        .note-content {
            min-height: 300px;
            padding: 15px;
            background-color: #2c2c2c;
            color: #f1f1f1;
            border-radius: 5px;
            border: 1px solid #444;
        }

        .btn-secondary {
            background-color: #444;
            border: none;
        }

        .btn-warning {
            background-color: #ffc107;
            color: #000;
        }

        a,
        a:hover {
            color: #f1f1f1;
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- Header + Navbar -->
        <%- include('header') %>
            <%- include('navbar') %>

                <!-- Main content -->
                <div class="card card mt-3">
                    <div class="card-header">
						<h3>
							<%= title %>
						</h3>
					</div>
                    <div class="card-body">
                        <div class="mb-4">
                            <label class="form-label fw-bold">Tiêu đề</label>
                            <div class="form-control p-3" readonly>
                                <strong>
                                    <%= ghichu.TieuDe %>
                                </strong>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">Nội dung ghi chú</label>
                            <div class="note-content"><%- ghichu.NoiDung %></div>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a href="/ghichu/cuatoi" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Quay lại
                            </a>
                            <a href="/ghichu/sua/<%= ghichu._id %>" class="btn btn-warning">
                                <i class="bi bi-pencil-square"></i> Sửa ghi chú
                            </a>
                        </div>
                    </div>
                </div>

                <%- include('footer') %>
    </div>

    <%- include('javascript') %>
</body>

</html>