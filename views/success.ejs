<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet" />
    <title>
        <%= title %> - Quản lý ghi chú
    </title>
    <style>
        body {
            background-color: #1c1e21;
            /* Nền tối */
            color: #f8f9fa;
            /* Chữ sáng */
        }

        .card {
            background-color: #1c1e21;
            border: none;
        }

        .card-header {
            border-bottom: 1px solid rgb(0, 255, 255);
            background: #12121293;
            color: #17b945;
            font-weight: bold;
        }

        .alert-success {
            background-color: #198754;
            color: #fff !important;
        }

        #message {
            color: #fff !important;
        }
    </style>
</head>

<body>
    <div class="container">
        <%- include('header') %>
            <!-- Menu ngang -->
            <%- include('navbar') %>

                <!-- Nội dung chính -->
                <div class="card mt-3">
                    <div class="card-header">
                        <h3>
                            <%= title %>
                        </h3>
                    </div>
                    <div class="card-body">
                        <% if (typeof message !=="undefined" && message) { %>
                            <div class="alert alert-success d-flex align-items-center px-4 py-3 rounded-3 border-0"
                                role="alert" style="background-color: #ffffff; color: #198754;">
                                <i class="bi bi-check-circle-fill me-2 fs-5" style="color: #198754;"></i>
                                <div class="fw-semibold text-white">
                                    <%- message %>
                                </div>
                            </div>
                            <% } %>

                    </div>
                </div>

                <!-- Footer -->
                <%- include('footer') %>
    </div>

    <%- include('javascript') %>
</body>

</html>